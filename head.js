// we don't need assertEqual here as we are testing head.js in headTest.js file
// const assertEqual = require('./assertEqual');

const head = function(array) {
  return array[0];
}

module.exports = head;